1. Объясните, что имеется в виду, когда говорится: Java-язык программирования и Java-платформа.
Java язык программирования -- статически типизированный объектно-ориентированный язык программирования.
Java-платформа -- это набор программных продуктов, которые позволяют создавать и использовать приклодное программное обеспечение, а также использовать его в любой системе, так как Java кроссплатформенный язык.
2. Поясните, как связаны имя java-файла и классы, которые в этом файле объявляются.
Имя должно совпадать с паблик-классом в этом файле
В файле может быть только один public класс, но сколь угодно default классов
3. Расшифруйте аббревиатуры JVM, JDK и JRE; покажите, где “они находятся” и что собой представляют.
JVM - виртуальная машина джава, является частью jre. Исполняет байт-код java, созданный самой Javой
JDK - набор для разработки на java. включает в себя jre и jvm.
JRE - набор для выполнения java приложений, без компилятора.

4. Объясните, как скомпилировать и запустить приложение из командной строки, а также зачем в переменных
среды окружения прописывать пути к установленному jdk.
путь к JDK нужно прописывать для того, чтобы runtime env выбрал именно тот jdk, который ему нужен
на компьютере может быть установлено несколько jdk, например 8 и 14. И разработчику необходимо выбрать именно тот, через который он хочет запустить код
комплияция javac Main.java
запуск java Main

5. Перечислите атрибуты доступа, объясните их действие.
public - доступ во всём проекте
private - доступ только из того же класса
package - доступ только из того же пакета
default - такой же, как и у package, но используется в пакете, где есть какието классы утилиты, не реализующие функциональность всех остальных классов в пакете
6. Что такое пакеты в java-программе, что представляют собой пакеты на диске? Каково соглашение по
именованию пакетов? Как создать пакет?
пакеты в java - это папки, в которых хранятся файлы-классы. на диске пакеты представляют  собой просто папку. чтобы создать пакет - достаточно создать папку.
Префикс уникального имени пакета всегда пишется в нижнем регистре латиницей и должен соответствовать одному из доменов высшего уровня

7. Объясните, какие классы, интерфейсы, перечисления необходимо импортировать в вашу программу, как
это сделать. Влияет ли импорт пакета на импорт классов и др., лежащего в подпакетах? Какой пакет в Java
импортируется по умолчанию?
импортировать нужно все классы, интерфейсы и перечисления, которые находятся в другом пакете. делается это одной строчкой кода: import [имя пакета].[имя класса];
также можно использовать звёздочку, чтобы импортировать все классы

8. Объясните различия между терминами “объект” и “ссылка на объект”.
объект в java -- экземпляр какого-то класса, хранящийся в памяти
ссылка на объект -- ссылка, указывающая на место в памяти, где хранится этот самый экземпляр
пример: Main main = new Main()
main - является ссылкой
new Main() - выделение места в памяти, под объект.

9. Какие примитивные типы Java вы знаете, как создать переменные примитивных типов? Объясните
процедуру, по которой переменные примитивных типов передаются в методы как параметры.
byte short int boolean long char double float
переменные примитивных типов данных создаются так: int g;
также можно сразу инициализировать ее: int g = 123;
в качестве методов передаются не сами эти переменные, а их копии. То есть при изменении в методе передаваемой ему переменной,  в месте вызова этого метода переменная не изменится.

10. Каков размер примитивных типов, как размер примитивных типов зависит от разрядности платформы, что
такое преобразование (приведение) типов и зачем оно необходимо? Какие примитивные типы не приводятся
ни к какому другому типу.
boolean 1bit
char    16bits
byte	32bits
short	64bits
int	32bits
long	64bits
float	32bits
double	64bits
приведение типов данных это изменение типа. использовать его можно, например, чтобы сэкономить место в какой то части проекта
если у нас есть значение int и оно равно 120, и мы точно уверены, что в данном участке кода увеличиваться больше 127 оно не будет
мы его можем привести к значению byte
int k = 120;
byte variable = (byte)k; 
11. Объясните, что такое явное и неявное приведение типов, приведите примеры, когда такое преобразование
имеет место.
явное приведение типов:
int k = 120;
byte variable = (byte)k; 
спользовать его можно, например, чтобы сэкономить место в какой то части проекта
если у нас есть значение int и оно равно 120, и мы точно уверены, что в данном участке кода увеличиваться больше 127 оно не будет
мы его можем привести к значению byte
неявное приведение происходит автоматически. они могут происходит с потерей точности.
возможные преобразования:
byte > short > int > long
int > double
short > float > double
char > int
12. Что такое литералы в Java-программе, какую классификацию литералов вы знаете, как записываются
литералы различных видов и типов в Java-программе?
Литералы — это явно заданные значения в коде программы. например какой-то литерал типа String будет выглядеть так: "Литерал стринга"
Литерал типа int выглядит как простое число 43, 312, 2, 5, 8
13. Как осуществляется работа с типами при вычислении арифметически выражений в Java?
При выполнении арифметических операций стоит обратить внимание на типы данных, не стоит использовать byte или short для больших значений 
14. Что такое классы-оболочки, для чего они предназначены? Объясните, что значит: объект класса оболочки
– константный объект.
Классы оболочки копируют то, что уже существует, но добавляет новые возможности для работы с примитивными типами данных так, как с ссылочными. 
Объект класса оболочки - константный объеккт значит, что объект является не изменяемым, то есть инициализирован с модификатором final
15. Объясните разницу между примитивными и ссылочными типами данных. Поясните существующие различия,
при передаче параметров примитивных и ссылочных типов в методы. Объясните, как константные объекты
ведут себя при передаче в метод.
примитивные типы данных инициализируются и хранятся в памяти, ссылочные типы хранятся в памяти, но чтобы взаимодействовать с ними мы имеем ссылку

16. Поясните, что такое автоупаковка и автораспаковка.
Автоупаковка - автоматическое приведение примитивных типов данных к их классам-обёртками. Integer integer = 9;
автораспаковка -наоборот.
int in = 0;
in = new Integer(9);

17. Перечислите известные вам арифметические, логические и битовые операторы, определите случаи их
употребления. Что такое приоритет оператора, как определить, в какой последовательности будут
выполняться операции в выражении, если несколько из них имеют одинаковый приоритет.
! — оператор отрицания;
&& — оператор логическое И (сокращенный);
|| — оператор логическое ИЛИ (сокращенный);
& — оператор побитовое И;
| — оператор побитовое ИЛИ;
^ — оператор побитовое исключающее ИЛИ.

18. Укажите правила выполнения операций с плавающий точкой в Java (согласно стандарту IEEE754). Как
определить, что результатом вычисления стала бесконечность или нечисло?
представление действительных чисел должно записываться в экспоненциальном виде
проверить, равен ли результат Double.NEGATIVE_INFINITY или Double.POSITIVE_INFINITY

19. Что такое статический импорт, какие элементы можно импортировать при статическом импорте.
Оператор import, предваряемый ключевым словом static, можно применять для импорта статических членовькласса или интерфейса. Благодаря статическому импорту появляется возможность ссылаться на статические члены непосредственно по именам, не угочняя ихименем класса.
то есть можно писать не Math.sqrt
а просто sqrt. 
можно импортить только статик методы

20. Объясните работу операторов if, switch, while, do-while, for, for-each. Напишите корректные примеры
работы этих операторов.
if(a == b){}

switch{
    case 1:
	break;
    case 2:
	break;
}

while(true){}

do{
}while(true);
for(int i = 0; i < 100; i ++){
}
for(Integer t : arraylist){
}
21. Объясните работу оператора instanceof. Что будет результатом работы оператора, если слева от него будет
стоять ссылка, равная null?
instanceof возвращает true, если оба объекта являются типом одного класса.
false